import 'package:flutter/material.dart';

class AppColors {
  static const black = Color(0xFF1B1620);       // warm near-black, less flat than pure black
  static const plumDark = Color(0xFF2A2130);    // deep muted eggplant backdrop
  static const plum = Color(0xFF4A3B54);        // muted mauve-plum, desaturated (not bright purple)
  static const lightPurple = Color(0xFF9C8AAE); // dusty, muted lilac accent (not neon)
  static const lavender = Color(0xFFEDE7E2);    // warm off-white, not pastel-pink-purple
  static const cream = Color(0xFFF4EFE6);       // warm parchment tone
  static const grey = Color(0xFF8B8290);        // muted neutral grey
  static const white = Color(0xFFFFFFFF);
  static const line = Color(0xFF5A4C64);        // muted border, matches new plum family
}